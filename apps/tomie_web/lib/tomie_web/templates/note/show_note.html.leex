<nav>
  <ul>
    <li><%= live_patch "Back", to: Routes.live_path(@socket, NoteLive.ShowNotebook, @notebook_id) %></li>
    <li><%= live_patch "Edit", to: Routes.live_path(@socket, NoteLive.EditNote, @notebook_id, @note) %></li>
    <li><%= live_patch "Delete", to: "#", phx_click: "delete_note", phx_value_id: @note.id, data: [confirm: "Are you sure?"] %></li>
  </ul>
</nav>

<%= live_component @socket, TomieWeb.NoteLive.NoteComponent, note: @note %>
